#import <Square>
#import { Background } from 'Utils/BackGround.dkit' 

export Square('BackGorund') {
   if(isHover()) rounded: 60;

    if(isHover()) {
      ifj(isActive() && isClick()) {
         color: rgb(155,255,255);
       }
   }
   color: hex("#FFFFFF");
   width: 100%;
   height: 100%;
 }

export Square('Sqd') {
   color: hex("#FFFFFF");
   width: 100px;
   height: 100px;
 }
